package com.staff_devices;
import com.alerts.Alert;
/**
 * The {@code StaffDevice} interface is used to display alerts to staff members.
 */
public interface StaffDevice {
    public void displayAlert(Alert alert);
}